package week4

object subtyping {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(77); 
  println("Welcome to the Scala worksheet");$skip(53); 
  
  val a: Array[NonEmpty] = new Array(new NonEmpty;System.out.println("""a  : Array[<error>] = """ + $show(a ))}
}
